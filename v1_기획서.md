# 주식 정보 앱 — v1 개발 기획서 (무료 스택 기준)

> 가칭: "내 종목 한눈에" · 작성 기준: 비용 0원 출시(Android 먼저)

---

## 1. 한 줄 컨셉

국내·해외 관심종목의 **공시·공모주(상장) 일정**을 한 화면에 모아 보여주고, 중요한 변화는 **D-day 푸시 알림**으로 먼저 알려주는 앱.

핵심 리텐션 루프: `푸시 알림 → 앱 진입 → 관심종목 확인`. 이 루프가 매일 돌게 만드는 것이 v1의 유일한 목표다.

---

## 2. v1 범위 (확정)

### 꼭 넣는 것 (v1)
- 관심종목 등록 — 국내(종목코드)·해외(티커)를 같은 흐름에서 추가
- 공시 알림 — 국내 DART 공시 + 미국 8-K(EDGAR)
- 공모주/상장 일정 — 청약 시작·마감·환불·상장일을 캘린더로, D-day 푸시
- 계정/로그인 — 관심종목·알림설정을 서버에 저장 (나중에 웹과 자동 공유)

### 뒤로 미루는 것 (v1.5 ~ v2)
- 배당 관리 / 배당 캘린더
- 실적 캘린더
- 뉴스 피드 (제목 + 출처 + 링크 + 요약 형태)
- 실시간 시세
- 웹 버전

> 미루는 기능들은 데이터 소스가 v1과 같아서, 백엔드만 잘 잡아두면 빠르게 얹을 수 있다.

---

## 3. 화면 구조 (정보 구조도)

```
온보딩 / 로그인
   │
   ├─ 홈 (관심종목 리스트)
   │     └─ 종목 추가 / 검색 (국내·해외 통합)
   │     └─ 종목 상세 (공시·일정 모아보기)
   │
   ├─ 공시 (피드: 내 종목 공시 + AI 한 줄 요약)
   │
   ├─ 캘린더 (공모주·상장 일정, 날짜순 / 필터: 공모주·상장)
   │
   └─ 설정
         ├─ 알림 설정 (어떤 이벤트를 푸시할지)
         └─ 계정
```

하단 탭: `홈 · 공시 · 캘린더 · 설정` (배당/실적 탭은 v2에서 추가)

---

## 4. 아키텍처 (앱 먼저, 웹은 같은 백엔드에 얹기)

```
외부 데이터
  DART(공시·증권신고서) · EDGAR(미국 8-K) · Finnhub(IPO·시세) · KIS(시세) · KRX KIND(상장일정)
        │
        │  ① 서버가 주기적으로 수집·가공 (앱이 직접 호출하지 않음)
        ▼
  우리 백엔드  =  Supabase (Postgres + Auth) + 수집기/스케줄러 + FCM 발송
        │                                   │
        │ ② 우리 API만 호출                  │ ③ 변화 감지 시 푸시
        ▼                                   ▼
   모바일 앱 (Flutter, v1)            웹 (Flutter Web, v2 — 같은 API/계정 재사용)
```

설계 원칙 3가지:
1. **데이터 수집은 앱이 아니라 서버가 한다.** API 키 노출·중복 호출·rate limit을 서버 한 곳에서 통제.
2. **인증·데이터는 처음부터 앱·웹 공용.** 관심종목·알림설정이 서버에 있으면, 웹에서 같은 계정으로 로그인 시 자동으로 이어진다. 이것이 "앱 기반 웹 연동"의 실체.
3. **비즈니스 로직은 서버에만.** 웹을 붙일 때 화면만 새로 그리면 된다.

---

## 5. 무료 데이터 소스

| 용도 | 소스 | 무료 | 비고 |
|---|---|---|---|
| 국내 공시·증권신고서 | DART OpenAPI (금감원) | ✅ | 인증키 무료 발급, 상업적 이용 가능 |
| 국내 공모주/상장 일정 | DART 증권신고서 + KRX KIND | ✅ | 청약가·일정은 신고서, 신규상장일은 KIND |
| 미국 공시 (8-K 등) | SEC EDGAR API | ✅ | 공식·무료, 키 불필요 (User-Agent 헤더만) |
| 미국 IPO 캘린더 | Finnhub `/calendar/ipo` | ✅ | 무료 티어 (분당 60회) |
| 시세 (국내·해외) | KIS Developers / Finnhub | ✅ | KIS는 계좌+키 필요, Finnhub 무료 티어 |
| 배당 지표 (v2) | Finnhub `/stock/metric` | ✅ | 배당수익률 등. 정확한 배당락일은 보강 필요 |

> 주의: DART는 종목코드가 아니라 8자리 `corp_code`를 쓴다. `corpCode.xml`(전 종목 매핑 파일)을 한 번 받아 DB에 저장해두고 변환한다.

---

## 6. 무료 기술 스택

| 영역 | 선택 | 비용 | 메모 |
|---|---|---|---|
| 앱 프레임워크 | **Flutter** | 0원 | 한 코드베이스로 Android·iOS·Web → "웹은 나중에"와 완벽히 맞음 |
| 백엔드/DB/인증 | **Supabase** (Postgres + Auth) | 0원 | 500MB DB, 50k MAU, 무제한 API 요청 |
| 서버 로직 | Supabase Edge Functions 또는 작은 Node 서버 | 0원 | 월 50만 호출 무료 |
| 푸시 | **FCM** (Firebase Cloud Messaging) | 0원 | Android·iOS 모두 무료·무제한 |
| 스케줄러(수집/푸시 트리거) | **GitHub Actions cron** | 0원 | public repo면 무료. Supabase 일시정지도 동시에 방지 |
| 소개 페이지(선택) | GitHub Pages / 무료 서브도메인 | 0원 | 마케팅·ASO용. "웹앱"과는 다른 가벼운 한 장 |

---

## 7. 비용, 솔직하게

진짜로 0원인 것: 데이터 API 전부, Supabase, FCM, GitHub Actions, Flutter, Google Play(이미 등록 완료된 25달러 1회 비용).

**유일하게 불가피한 비용:** Apple Developer 멤버십 **연 99달러**. iOS 출시·테스트에 필수다.

→ 그래서 v1은 **Android 먼저** 출시하고, iOS는 다운로드/반응이 붙거나 약간의 매출이 나오면 그때 99달러를 쓰는 걸 권한다. Flutter라서 iOS 추가는 코드 거의 그대로 빌드 타깃만 늘리면 된다.

무료 티어의 함정도 알아두기:
- Supabase는 **7일 무요청 시 일시정지** → GitHub Actions cron이 주기적으로 호출하니 자연히 해결.
- Supabase 무료엔 **자동 백업 없음** → GitHub Actions로 주기 백업 설정 권장.
- Finnhub 무료는 **분당 60회** → 서버가 모아서 DB에 캐시(앱이 직접 호출 금지).

---

## 8. 개발 순서 (체크리스트)

**1단계 — 백엔드 뼈대 (먼저)**
- [ ] DART·Finnhub 무료 키 발급
- [ ] 동봉한 백엔드 스타터 실행 → 공시·IPO 데이터 받아지는지 확인
- [ ] Supabase 프로젝트 생성, 테이블 설계(users, watchlist, disclosures, ipo_events, devices)
- [ ] `corpCode.xml` 받아 종목코드↔corp_code 매핑 저장

**2단계 — 수집 + 알림**
- [ ] 수집기: 관심종목들의 신규 공시/IPO 일정 폴링 → DB 저장 → 신규분 감지
- [ ] FCM 연동: 신규 공시·D-day 발생 시 해당 사용자 기기로 푸시
- [ ] GitHub Actions cron으로 수집기 주기 실행

**3단계 — 앱 (Flutter)**
- [ ] 로그인(Supabase Auth) + 기기 토큰(FCM) 등록
- [ ] 홈/종목추가 → 캘린더 → 공시 피드 → 알림설정 화면 (목업 기준)
- [ ] 우리 API 연동, 푸시 수신 처리

**4단계 — 출시**
- [ ] Play Store 내부테스트 → 비공개 → 정식
- [ ] (이후) iOS, 웹(Flutter Web) 순차 확장

---

## 9. 리스크 / 반드시 지킬 것

- **유사투자자문업:** AI는 "의견/추천" 금지, **요약·설명만**. "왜 중요한지"까지만, "사라/오른다"는 절대 X.
- **저작권:** 뉴스 전문 재현 금지. 제목 + 출처 + 링크 + 직접 요약만.
- **API 키 보안:** KIS·Finnhub 키는 **서버에만**. 앱에 넣지 않는다.
- **rate limit:** 외부 호출은 서버가 모아서 캐시. 사용자 증가가 외부 호출 증가로 직결되지 않게.
- **법적 확인:** 위 규제 항목은 출시 전 금융 관련 법무 전문가에게 한 번 확인 권장. (본 문서는 법률 자문이 아님)
